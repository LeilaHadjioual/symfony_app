{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Projet creation</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-primary" type="submit" value="Create"/>
        {{ form_end(form) }}

        <a href="{{ path('projet_index') }}"><button type="button" class="btn btn-info">Back to the list</button></a>


    </div>
{% endblock %}
